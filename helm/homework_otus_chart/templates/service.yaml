apiVersion: v1
kind: Service
metadata:
  name: {{ include "homework_otus_chart.fullname" . }}
  labels:
    {{- include "homework_otus_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homework_otus_chart.selectorLabels" . | nindent 4 }}
