apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "homework_otus_chart.fullname" . }}-test-connection"
  labels:
    {{- include "homework_otus_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "homework_otus_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
